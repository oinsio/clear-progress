# Delta Spec: Goal Focus — fix-goal-status-edit-mode

The `goal-focus` specification already contains correct requirements for this change (Requirement: Auto-cleanup of invalid focused goals). The implementation doesn't match the specification — this is a bug fix, not a requirements change.

Affected scenarios from the main specification (no changes):
- "Status changed to completed/cancelled during editing (not saved yet)"
- "Status changed to completed/cancelled, then edit cancelled"
- "Status changed to completed/cancelled, then saved"

## MODIFIED Requirements

No changes to requirements. Main specification is correct.
