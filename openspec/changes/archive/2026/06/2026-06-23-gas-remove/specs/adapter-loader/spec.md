## REMOVED Requirements

### Requirement: Adapter loading and switching
**Reason**: Package was already deprecated (D3 of add-supabase-ui). Adapter creation is handled directly in `defaultServices.ts`. Removing the empty package.
**Migration**: None needed — already migrated.
