# Prod defaults — safe to commit (placeholders only)
# Real values go into .env.prod.local (gitignored) or CI env vars
VITE_BASE_PATH=/clear-progress/
VITE_SUPABASE_URL=https://your-prod-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-prod-anon-key
