{
  "name": "clear-progress",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @clear-progress/client dev",
    "prebuild": "pnpm lint:fix",
    "build": "pnpm --filter @clear-progress/contract build && pnpm --filter @clear-progress/adapter-inmemory build && pnpm --filter @clear-progress/adapter-supabase build && pnpm --filter @clear-progress/client build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm --filter @clear-progress/contract build && pnpm --filter @clear-progress/adapter-inmemory build && pnpm --filter @clear-progress/adapter-supabase build && pnpm -r typecheck",
    "test:mutation": "pnpm -r test:mutation",
    "i18n:check": "pnpm --filter @clear-progress/client i18n:check",
    "preflight": "pnpm lint && pnpm typecheck && pnpm test && pnpm i18n:check"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "^6.4.0",
    "@biomejs/biome": "^2.4.13",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
