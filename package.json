{
  "name": "clear-progress-gtd",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @clear-progress/client dev",
    "prebuild": "pnpm lint:fix",
    "build": "pnpm --filter @clear-progress/contract build && pnpm --filter @clear-progress/adapter-gas build && pnpm --filter @clear-progress/adapter-inmemory build && pnpm --filter @clear-progress/adapter-loader build && pnpm --filter @clear-progress/client build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm --filter @clear-progress/contract build && pnpm --filter @clear-progress/adapter-gas build && pnpm --filter @clear-progress/adapter-inmemory build && pnpm --filter @clear-progress/adapter-loader build && pnpm -r typecheck",
    "test:mutation": "pnpm -r test:mutation",
    "preflight": "pnpm lint && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "^6.4.0",
    "@biomejs/biome": "^2.4.13",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
